{
  "name": "serverless-offline-streams-example",
  "version": "1.0.1",
  "main": "../../dist/index.js",
  "scripts": {
    "compile": "tsc --build",
    "compile:watch": "tsc --build -w",
    "sls:run:local": "export IS_OFFLINE=true && sls offline start --useWorkerThreads --lambdaPort 3003 --stage local"
  },
  "dependencies": {
  },
  "devDependencies": {
    "serverless-offline-streams": "*",
    "@serverless/typescript": "^3.1.1",
    "@types/serverless": "^3.0.0",
    "serverless": "2.72.0",
    "serverless-offline": "^8.3.1",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  },
  "engines": {
    "node": ">=10.0"
  },
  "license": "MIT"
}
