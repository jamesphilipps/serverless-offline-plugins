{
  "name": "serverless-offline-streams-parent",
  "private": true,
  "version": "1.0.1",
  "scripts": {
    "compile": "tsc --build",
    "compile:watch": "tsc --build -w"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "engines": {
    "node": ">=10.0"
  },
  "license": "MIT"
}
